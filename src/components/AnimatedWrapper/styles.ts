import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    marginBottom: '50%',
  },
  lottie: {
    width: '80%',
    aspectRatio: 1,
  },
  title: {
    fontSize: 20,
    color: 'whitesmoke',
    fontWeight: 'normal',
  },
})
